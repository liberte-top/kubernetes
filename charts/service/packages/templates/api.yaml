apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "packages.componentName" (dict "root" . "component" "api") }}
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ include "packages.componentName" (dict "root" . "component" "api") }}
{{ include "packages.labels" (dict "root" . "component" "api") | indent 4 }}
spec:
  replicas: {{ .Values.api.replicaCount }}
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: {{ include "packages.componentName" (dict "root" . "component" "api") }}
  template:
    metadata:
      labels:
        app: {{ include "packages.componentName" (dict "root" . "component" "api") }}
{{ include "packages.labels" (dict "root" . "component" "api") | indent 8 }}
{{- with .Values.podAnnotations }}
      annotations:
{{ toYaml . | indent 8 }}
{{- end }}
    spec:
{{- if .Values.imagePullSecrets }}
      imagePullSecrets:
{{- range .Values.imagePullSecrets }}
        - name: {{ . }}
{{- end }}
{{- end }}
      containers:
        - name: api
          image: {{ .Values.api.image.repository }}:{{ .Values.api.image.tag }}
          imagePullPolicy: {{ .Values.api.image.pullPolicy }}
          env:
{{- range $key, $value := .Values.api.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
{{- end }}
          envFrom:
            - secretRef:
                name: {{ .Values.api.secretName }}
          ports:
            - containerPort: {{ .Values.api.port }}
              name: http
          readinessProbe:
            httpGet:
              path: {{ .Values.api.readinessProbe.path }}
              port: {{ .Values.api.port }}
            initialDelaySeconds: {{ .Values.api.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.api.readinessProbe.periodSeconds }}
          livenessProbe:
            httpGet:
              path: {{ .Values.api.livenessProbe.path }}
              port: {{ .Values.api.port }}
            initialDelaySeconds: {{ .Values.api.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.api.livenessProbe.periodSeconds }}
          resources:
{{ toYaml .Values.api.resources | indent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "packages.componentName" (dict "root" . "component" "api") }}
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ include "packages.componentName" (dict "root" . "component" "api") }}
{{ include "packages.labels" (dict "root" . "component" "api") | indent 4 }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "packages.componentName" (dict "root" . "component" "api") }}
  ports:
    - name: http
      port: {{ .Values.api.service.port }}
      targetPort: {{ .Values.api.port }}
