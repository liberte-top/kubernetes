apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "crates.componentName" (dict "root" . "component" "api") }}
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ include "crates.componentName" (dict "root" . "component" "api") }}
{{ include "crates.labels" (dict "root" . "component" "api") | indent 4 }}
spec:
  replicas: {{ .Values.api.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "crates.componentName" (dict "root" . "component" "api") }}
  template:
    metadata:
      labels:
        app: {{ include "crates.componentName" (dict "root" . "component" "api") }}
{{ include "crates.labels" (dict "root" . "component" "api") | indent 8 }}
{{- with .Values.podAnnotations }}
      annotations:
{{ toYaml . | indent 8 }}
{{- end }}
    spec:
{{- if .Values.imagePullSecrets }}
      imagePullSecrets:
{{- range .Values.imagePullSecrets }}
        - name: {{ . }}
{{- end }}
{{- end }}
      containers:
        - name: api
          image: {{ .Values.api.image.repository }}:{{ .Values.api.image.tag }}
          imagePullPolicy: {{ .Values.api.image.pullPolicy }}
          env:
{{- range $name, $ref := .Values.api.envSecretRefs }}
            - name: {{ $name }}
              valueFrom:
                secretKeyRef:
                  name: {{ $ref.secretName }}
                  key: {{ $ref.secretKey }}
                  optional: {{ $ref.optional }}
{{- end }}
{{- range $key, $value := .Values.api.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
{{- end }}
          ports:
            - containerPort: {{ .Values.api.port }}
              name: http
          readinessProbe:
            httpGet:
              path: {{ .Values.api.readinessProbe.path }}
              port: {{ .Values.api.port }}
            initialDelaySeconds: {{ .Values.api.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.api.readinessProbe.periodSeconds }}
          livenessProbe:
            httpGet:
              path: {{ .Values.api.livenessProbe.path }}
              port: {{ .Values.api.port }}
            initialDelaySeconds: {{ .Values.api.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.api.livenessProbe.periodSeconds }}
          resources:
{{ toYaml .Values.api.resources | indent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "crates.componentName" (dict "root" . "component" "api") }}
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ include "crates.componentName" (dict "root" . "component" "api") }}
{{ include "crates.labels" (dict "root" . "component" "api") | indent 4 }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "crates.componentName" (dict "root" . "component" "api") }}
  ports:
    - name: http
      port: {{ .Values.api.service.port }}
      targetPort: {{ .Values.api.port }}
