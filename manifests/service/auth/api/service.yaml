apiVersion: v1
kind: Service
metadata:
  name: auth-api
  namespace: service
  labels:
    app: auth-api
spec:
  type: ClusterIP
  selector:
    app: auth-api
  ports:
    - name: http
      port: 3333
      targetPort: 3333
