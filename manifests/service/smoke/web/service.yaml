apiVersion: v1
kind: Service
metadata:
  name: smoke-web
  namespace: service
  labels:
    app: smoke-web
spec:
  type: ClusterIP
  selector:
    app: smoke-web
  ports:
    - name: http
      port: 80
      targetPort: 80
