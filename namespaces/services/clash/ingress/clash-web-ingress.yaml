apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: clash-web
  namespace: services
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/auth-url: "https://auth.liberte.top/api/verify"
    nginx.ingress.kubernetes.io/auth-signin: "https://auth.liberte.top/login?redirectUrl=$scheme://$host$request_uri"
    nginx.ingress.kubernetes.io/auth-response-headers: "X-User-Id,X-User-Email,X-User-Roles"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - clash.liberte.top
      secretName: clash-tls
  rules:
    - host: clash.liberte.top
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: clash-web
                port:
                  number: 80
